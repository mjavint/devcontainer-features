{
    "name": "uv/uvx",
    "id": "uv",
    "version": "1.0.0",
    "description": "Tool to replace pip, pip-tools, pipx, poetry, pyenv, virtualenv, and more feature.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of UV to install. Accepts versions with 'v' prefix."
        }
    }
}
